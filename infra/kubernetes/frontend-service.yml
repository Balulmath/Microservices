apiVersion: v1
kind: Service
metadata:
  name: treasury-react-portal
  namespace: treasury
spec:
  type: LoadBalancer
  selector:
    app: treasury-react-portal
  ports:
    - port: 80
      targetPort: 5173
